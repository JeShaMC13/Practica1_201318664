import java.util.*;
public class Aleatorio {
	static void num(){
			Random a = new Random();
			int aleatorio = (int) (Math.random() * 10) + 1;
				Random rnd = new Random();
				System.out.println("Número aleatorio real entre [0,10] : "+aleatorio);	
		}
	    
		}
